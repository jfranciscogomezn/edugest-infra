# Contribución — edugest-infra

## Política de ramas

- Rama principal: `master` — **protegida**: solo merge vía Pull Request (branch protection + ruleset activos).
- Sin push directo, force-push ni borrado de `master`.
- Owner / revisor: `@jfranciscogomezn` (`.github/CODEOWNERS`).

Política global: repo [`edugest`](https://github.com/jfranciscogomezn/edugest) → `CONTRIBUTING.md`.
